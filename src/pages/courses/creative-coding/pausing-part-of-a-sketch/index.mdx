---
title: "Pausing Part of a Sketch"
layout: ../../../../layouts/CreativeCodingLayout.astro
---

import StepThroughTutorial from "../../../../components/StepThroughTutorial.astro";

export const steps = [
  {
    description: `<p>This sketch animates two objects: a circle that moves from left to right, and a square that spins in place.</p>
<p>Click the mouse button to pause them both. Click again to resume them.</p>
<p>This uses <code>noLoop()</code> to pause and <code>loop()</code> to resume. This is simple, but it doesn&rsquo;t give you much control over what is paused and what is resumed.</p>`,
    code: `function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

// veering circle
let x = (millis() / 5) % width;
let y = map(sin(millis() / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(millis() / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
if (isLooping()) {
noLoop();
} else {
loop();
}
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>First, some setup. We are going to need to distinguish between the real time (returned by <code>millis()</code>), and the animation time.</p>
<p>Introduce a variable that captures the value of <code>millis()</code> each frame, and use its value instead of calling <code>millis()</code>.</p>`,
    code: `let animationTime;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

animationTime = millis();

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(animationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
if (isLooping()) {
noLoop();
} else {
loop();
}
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>Introduce another variable, to tell whether to update <code>animationTime</code> from <code>millis()</code>. Modify <code>mousePressed</code> to change the value of this variable, instead of calling <code>loop()</code> and <code>noLoop()</code>.</p>`,
    code: `let animationTime;
let isAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (isAnimated) {
animationTime = millis();
}

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(animationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
if (isAnimated) {
isAnimated = false;
} else {
isAnimated = true;
}
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>Instead of an if statement, <code>mousePressed()</code> could also use the not operator (<code>!</code>), that maps <code>false</code> onto <code>true</code> and <code>true</code> onto <code>false</code>.</p>`,
    code: `let animationTime;
let isAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (isAnimated) {
animationTime = millis();
}

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(animationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
isAnimated = !isAnimated;
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>This gives us more flexibility. For example, we could keep the square spinning (by driving it from <code>millis()</code>), while pausing and resuming the veering circle.</p>`,
    code: `let animationTime;
let isAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (isAnimated) {
animationTime = millis();
}

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(millis() / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
isAnimated = !isAnimated;
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>Or, we can have separate control over different parts of the sketch.</p>
<p>Here, <code>mousePressed</code> toggles the animation state of the veering circle; <code>keyPressed</code> toggles the animation state of the spinning square.</p>`,
    code: `let circleAnimationTime;
let squareAnimationTime;
let circleIsAnimated = true;
let squareIsAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (circleIsAnimated) {
circleAnimationTime = millis();
}
if (squareIsAnimated) {
squareAnimationTime = millis();
}

// veering circle
let x = (circleAnimationTime / 5) % width;
let y = map(sin(circleAnimationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(squareAnimationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
circleIsAnimated = !circleIsAnimated;
}

function keyPressed() {
squareIsAnimated = !squareIsAnimated;
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>Let&rsquo;s return to the simpler version of the code, that controls the animation of everything.</p>
<p>You may notice that when you press the mouse to pause the animation, and then resume it later, the circle and square jump ahead.</p>
<p>(This was an issue with the original version of the sketch, that used <code>loop()</code> and <code>noLoop()</code>, as well.)</p>
<p>This is because the clock that <code>millis()</code> reads is still running, even when <code>isAnimated</code> is false. If we pause for five seconds and then set <code>isAnimated</code> back to true, the value of <code>animationTime</code> will jump ahead by 5000.</p>`,
    code: `let animationTime;
let isAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (isAnimated) {
animationTime = millis();
}

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(animationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
isAnimated = !isAnimated;
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>Here&rsquo;s a pattern that fixes this. It uses an additional variable to remember when the animation was paused, so that it can offset <code>millis()</code> by the amount of time that the animation stayed paused, in creating the synthetic time value for <code>animationTime</code>.</p>`,
    code: `let animationTime;
let animationDelta = 0;
let isAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (isAnimated) {
animationTime = millis() + animationDelta;
}

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(animationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
if (isAnimated) {
animationDelta += millis();
} else {
animationDelta -= millis();
}
isAnimated = !isAnimated;
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  },
  {
    description: `<p>Now that we have control over time, we can also change it in other ways. For example, pressing the up and down arrows speeds up and slows down the animation, by changing the relationship between the actual time (<code>millis()</code>) and the time that the veering circle and spinning square code &ldquo;see&rdquo;.</p>
<p>Note: In OpenProcessing.org, you will need to click on the canvas before the sketch is notified of your key strokes. (Before you click, the canvas does not have &ldquo;focus.&rdquo;) The first click will pause the animation, so you will actually need to click twice. Then you can use the up and down arrows to speed up or slow down the animation.</p>`,
    code: `let animationTime;
let animationDelta = 0;
let animationRatio = 1;
let isAnimated = true;

function setup() {
createCanvas(windowWidth, 250);
}

function draw() {
background(100, 50);

if (isAnimated) {
animationTime = (millis() + animationDelta) * animationRatio;
}

// veering circle
let x = (animationTime / 5) % width;
let y = map(sin(animationTime / 700), -1, 1, 25, height - 25);
circle(x, y, 50);

// spinning square
rectMode(CENTER);
rotateAbout(animationTime / 800, 200, height / 2);
rect(200, height / 2, 100, 100);
}

function mousePressed() {
if (isAnimated) {
animationDelta += millis();
} else {
animationDelta -= millis();
}
isAnimated = !isAnimated;
}

function keyPressed() {
if (keyCode === UP_ARROW) {
animationRatio *= 1.5;
} else if (keyCode === DOWN_ARROW) {
animationRatio /= 1.5;
}
}

function rotateAbout(angle, x, y) {
translate(x, y);
rotate(angle);
translate(-x, -y);
}`
  }
];

# Pausing Part of a Sketch

The alternative to `noLoop()`. This tutorial shows how to pause and resume individual parts of a sketch by controlling animation time, rather than stopping the entire draw loop.

Use the **◀ ▶ buttons** or **arrow keys** to step through the stages. **Click** inside the canvas to pause/resume, and **press keys** where noted. **Drag any number** in the code to adjust it live.

<StepThroughTutorial steps={steps} canvasHeight={250} animated={true} />

---

**Try it on OpenProcessing:** [Pausing Part of a Sketch](https://openprocessing.org/sketch/1031084)
